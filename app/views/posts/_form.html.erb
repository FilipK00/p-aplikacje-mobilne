<%= form_for [@petrol_station, @post_date, @post] do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :pb_price %>
    <%= form.text_field :pb_price, id: :post_pb_price %>
  </div>

  <div class="field">
    <%= form.label :diesel_price %>
    <%= form.text_field :diesel_price, id: :post_diesel_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>